#include <stdio.h>
#include <stdlib.h>

#define 

//circle one encompasses circle two return true

typedef struct{
  int radius,x,y;
}Circle;

int main(void){
  Circle flower;
  Circle hunter;
  while(scanf("%d %d %d %d %d %d",&hunter.radius,&hunter.x,&hunter.y,
    &flower.radius,&flower.x,&flower.y) != EOF){
    
  }

  return EXIT_SUCCESS;
}
